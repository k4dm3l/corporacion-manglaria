---
import "@/styles/global.css";

const { title = "Corporación Manglaria", content = "" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/icon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={content} />
		<title>{title}</title>
		<script is:inline>
			document.documentElement.classList.toggle(
				"dark",
				localStorage.theme === "dark" ||
					(!("theme" in localStorage) &&
						window.matchMedia("(prefers-color-scheme: dark)")
							.matches),
			);
		</script>
	</head>
	<body
		class="bg-gray-200/80 relative dark:bg-secondary-text text-secondary-text dark:text-secondary-dark-text"
	>
		<main>
			<slot />
		</main>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		position: relative;
		overflow-x: hidden;
	}

	/* html::after {
		content: "";
		width: 600px;
		height: 600px;
		border-radius: 100%;
		background-color: red;
		top: -400px;
		position: absolute;
		left: 50%;
		transform: translateX(-50%);
		z-index: -1;
		pointer-events: none;
		background: radial-gradient(
			circle,
			rgba(250, 246, 254, 0.4) 10%,
				rgba(250, 246, 254, 0.3) 25%,
			rgba(216, 187, 247, 0.2) 50%,
			rgba(120, 51, 189, 0.15) 75%,
			rgba(95, 41, 160, 0.05) 90%,
			rgba(56, 16, 91, 0) 100%
		);
		filter: blur(80px);
	}*/
</style>
